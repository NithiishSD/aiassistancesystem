# --- Local model runner client ---
ollama==0.3.3

# --- Cloud model clients (free tiers) ---
google-generativeai==0.8.3
groq==0.11.0

# --- Vector memory (used from Phase 5 onward, installed now to avoid re-setup) ---
chromadb==0.5.20

# --- Orchestration / async ---
python-dotenv==1.0.1

# --- System monitoring (for VRAM/contention checks, orchestrator phase) ---
psutil==6.1.0
pynvml==11.5.3

# --- Logging (structured JSON) ---
python-json-logger==2.0.7
